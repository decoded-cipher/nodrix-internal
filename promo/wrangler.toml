# Cloudflare Pages config for the nodrix promo site.
# The site itself is static (Astro → dist/); this file exists for the Pages
# Functions in functions/. Deploy with `npm run deploy`.
name = "nodrix"
pages_build_output_dir = "dist"
compatibility_date = "2025-05-01"
