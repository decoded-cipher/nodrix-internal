{
  "name": "@nodrix/collector",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Central, anonymous install/usage telemetry collector for self-hosted nodrix instances.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250501.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.0.0"
  }
}
