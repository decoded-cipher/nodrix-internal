# Copy to .env (gitignored) and fill in.

# Full URL of the MCP endpoint on your deployed worker.
MCP_URL=https://your.workers.dev/v1/mcp

# Bearer token from your nodrix Settings → Tokens page (owner-scoped for
# all projects, or project-scoped to limit visibility).
MCP_BEARER=
